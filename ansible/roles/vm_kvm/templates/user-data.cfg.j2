#cloud-config
hostname: {{ vm_name }}
manage_etc_hosts: true
users:
  - name: {{ vm_user }}
    ssh-authorized-keys:
      - {{ vm_ssh_key }}
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    shell: /bin/bash
